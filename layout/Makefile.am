installdir = ${coredir}/layout
install_DATA = \
	__init__.py	 \
        HashPQueue.py    \
	LayoutObject.py  \
	Unit.py

EXTRA_DIST = $(install_DATA)
install_PYTHON = $(install_DATA)
CLEANFILES = *.pyc
