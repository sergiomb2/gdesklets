installdir = ${coredir}/factory
install_DATA = \
	__init__.py	  \
	ControlFactory.py \
	DisplayFactory.py \
	SensorFactory.py

EXTRA_DIST = $(install_DATA)
install_PYTHON = $(install_DATA)
CLEANFILES = *.pyc
