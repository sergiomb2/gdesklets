installdir = ${coredir}/shell/plugins/PackageInstaller
install_DATA = \
	__init__.py	\
	Downloader.py	\
	Package.py	\
	download.png

EXTRA_DIST = $(install_DATA)
install_PYTHON = $(install_DATA)
CLEANFILES = *.pyc
